name: Test Project
on: push
jobs:
  letsTest:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3.5.0
        with:
          clean: "true"
      - name: gettiong Codec
        run: |
            echo ">>> ${{ toJSON(github) }} <<<"
            echo ">>> --------------------- <<<"
            echo ">>> ${{ toJSON(secrets) }} <<<"
      - name: realTest
        run: npm run test         